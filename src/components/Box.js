import styled from 'styled-components';
import { background, color, space, layout, flexbox, border, shadow, grid } from 'styled-system';

export const Box = styled('div')(background, color, flexbox, layout, space, border, shadow, grid);